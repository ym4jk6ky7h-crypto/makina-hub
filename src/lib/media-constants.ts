/** Duración mínima de una sesión DJ (segundos). */
export const MIN_SESSION_SECONDS = 15 * 60;

/** Rango típico de un tema completo (excluye shorts y sesiones largas). */
export const MIN_TRACK_SECONDS = 2 * 60;
export const MAX_TRACK_SECONDS = 12 * 60;
