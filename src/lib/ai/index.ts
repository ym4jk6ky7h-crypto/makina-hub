/**
 * Capa de IA — preparada para integración futura.
 * Implementación de chat/agentes: ver /services/ai y /agents
 */

export type { AIAgentContext, AIAgentRole, AIQueryRequest, AIQueryResponse } from "./types";
export { buildDatabaseContextForAI } from "./context";
